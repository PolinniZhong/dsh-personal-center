# 变更日志(Changelog)

本项目的版本历史。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/),版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.8.0] - 2026-08-23

### 新增(条件注入 + 模板导入导出,PRD:docs/个性化指令增强-v0.8-PRD.md)
- **条件注入**:`rules` 数据结构(`{model?, timeStart?, timeEnd?, text}`),在「全局指令」子区配置;按 **模型**(单字段:空=任意 / `provider::model` 精确 / 仅模型名后缀匹配)与 **时段**(北京时间,支持跨天 `start>end`)自动追加指令;合并优先级 **条件 > 工作区 > 全局**;
- **模板导出 JSON**:一键导出(下载 `.json` 为主 + 剪贴板复制降级);
- **模板导入 JSON**:粘贴 → 校验 → **跳过同名**(默认不覆盖)→ 结果提示(导入 N / 跳过 M);
- **模板分享**:模板行「分享」按钮,复制模板为可读文本(clipboard 不可用时降级选中复制);
- 兼容:未配置规则时注入行为与 v0.7 完全一致;纯本地零新依赖。

## [0.7.0] - 2026-08-22

### 新增(个性化指令增强,PRD:docs/个性化指令增强-PRD.md)
- **按工作区指令**:新命名空间 `personal-center-instructions`(JSON 字符串,同 pricing 先例,坏 JSON 容错);注入按会话 **cwd 最长前缀匹配**工作区,有匹配注入「全局 + 工作区」合并文本,无匹配仅全局(与 v0.5.1 行为一致);子代理继承父 cwd 自动生效;
- **指令模板库**:内置 5 个模板(产品经理/开发者/写作/翻译/通用助手)+ 自定义模板(存为模板/编辑/删除),一键应用到全局或当前工作区;
- **注入预览**:个性化 tab 底部实时显示「全局 + 当前工作区」合并文本 + ≈token 估算,随切换联动;
- **个性化 tab 重构**:三子区(全局/按工作区/模板库)+ 工作区自动发现(`/personal-center/workspaces`)与手动添加、当前工作区高亮;
- 新路由:`GET/POST /personal-center/instructions`、`GET /personal-center/current-workspace`(最近活动会话 cwd,一次性非轮询)、`GET /personal-center/workspaces`。

### 平台适配(0.7.0 实测)
- **注入通道变更**:0.7.0 将 `systemPrompt` 服务收进 agent scope,第三方插件无法再通过 `systemPrompt.section/variable` 注册(注册回调不执行或抛错);改用 `system-prompt/assemble` **waterfall 事件**在组装结果里插入 custom-instructions 段(竞品 dsh-prompt-persona 同通道),实测注入生效(见 PLATFORM-NOTES §14);
- **失效预案**:事件监听 try/catch + 空值短路,通道失效自动降级为"不注入",不影响其它功能。

### 兼容
- 现有 `custom-instructions` 命名空间与数据**无缝延续**(不迁移、不丢配置);未配置任何工作区指令时行为与旧版完全一致;纯本地零网络零新依赖、不改核心。

## [0.6.0] - 2026-08-22

### 新增
- **会话状态概览 · 动作级实时状态**(对齐 Codex 桌面宠物):running 会话实时显示「正在读取 <文件>」/「正在执行命令 …」/「正在搜索」等动作文案;完成显示「就绪」绿徽标。
- **动作提取**:从 `manager.get(id).getSnapshot().nodes` 的最后一条 `kind:"tool-call"` 节点取 `name` + `argsRaw`(`read`/`write`/`edit` → `file_path` 末段;`bash` → `command` 前 24 字符);思考中无 tool-call 回落「运行中」;失败优先级最高,动作文案不覆盖。
- **实时性零新增**:沿用既有 `list.subscribe()` + `manager.get(id).subscribe()`,host `session/event` 帧(含 tool 事件)自动驱动重渲染。
- 纯客户端改动(零 host、零端点、零轮询);冒烟测试升级至 `/tmp/pet-client-smoke7.mjs`(30 项,含动作文案/就绪徽标/思考回落/失败优先断言)。

## [0.5.1] - 2026-08-22

### 修复
- **统计重复计数思考 token**:DSH 的 `outputTokens = completion_tokens` **已含**推理 token(`reasoningTokens` 是其子集,见 `dsh-llm-deepseek` 的 `mapUsage`),原统计 `input + output + cache + reasoning` 把思考部分算了两次(全量约多 267 万);现改为 `input + output + cache`。
- **成本重复计价(同因)**:输出按 `(output - reasoning)` 计输出价、思考单独计 `reasoning` 价(缺省同输出价),不再双重计费。
- 备注:与 DeepSeek 后台"总消耗"的差异主要不在本项——日志口径 98% 是缓存命中 token(全量 cacheRead ≈ 15.84 亿),后台口径需对照确认(见会话记录)。

## [0.5.0] - 2026-08-22

### 新增
- **宠物状态概览**:悬停宠物右上角「会话状态」按钮(或点击宠物)→ 毛玻璃浮层面板,实时展示会话状态(运行中 / 失败 / 已完成 / 待机),含摘要大数字(进行中 · 失败 · 完成)与会话列表(状态色圆点 + 徽标);点面板外或 ESC 关闭,无右上角关闭按钮。
- **数据源 = 平台已有投影**:`ctx.get("sessions")` 的 `list` + `manager.get(id)` 的 `lastAgentError`,零 host 改动、零新端点、零轮询:面板打开才订阅、关闭即退订并销毁 DOM(轻量约束:空闲零开销)。
- **失败如实显示**:会话最近一次 agent 交互失败(`lastAgentError`,host `agent/error` 事件驱动)即标红「失败」,绝不伪装绿色成功;优先级 失败 > 运行中 > 已完成 > 待机。
- **会话状态开启时隐藏数据气泡**:打开状态面板时不弹 Token 数据气泡(状态由面板列表展示),退出后恢复;宠物情绪随状态映射(失败→exhausted 着急 / 运行中→busy 忙碌 / 完成→happy 开心 / 空闲→dozing 打盹)。
- **懒挂载**:点击才创建面板 DOM,关闭即销毁;无 `sessions` 服务时点击仅保留原随机气泡行为,不阻塞宠物。
- 冒烟测试:`/tmp/pet-client-smoke7.mjs`(23 项断言:按钮/面板展开/隐藏气泡/关闭恢复/摘要/失败红色/菜单居中/宠物回归)。

## [0.4.22] - 2026-08-22

### 变更
- **宠物启用开关与会话库统一**:启用态轨道色由 `deepseek-400`(亮蓝)改为 `--dsw-alias-brand-primary`(品牌主色),与会话库 skb-switch 视觉一致(白圆钮 + 品牌色轨道)。

## [0.4.21] - 2026-08-22


### 修复
- **小黑鲸动画位移恢复**:尺寸归一化由「每帧居中」改为「统一尺寸、保留各帧角色位移」(以 bbox 中心为锚),蹦跳/颤抖等整体位移动画不再被抹平;角色高度统一 ~295px,素材版本升至 `?v=4` 强制刷新。

## [0.4.20] - 2026-08-22


### 变更
- **数据气泡改毛玻璃**:半透明背景 + `backdrop-filter: blur(12px) saturate(1.4)`(参考 Codex/苹果,透出背后界面并模糊);浅色 rgba(255,255,255,.6)/深色 rgba(28,30,36,.6),半透明描边与小尾巴同步;不支持 backdrop-filter 时降级为纯半透明。

## [0.4.19] - 2026-08-22


### 变更
- **桌面端浮层静止 = 睡觉**:启动后的宠物在静止(未播放动画)时统一显示「打盹(dozing)」表情;悬停/点击/情绪变化/唤醒时按当前情绪播放动画,播完回到睡觉;设置面板列表保持原样(仍显示当前情绪静态图)。

## [0.4.18] - 2026-08-22


### 修复
- **小黑鲸显示尺寸过小(比蓝鲸小约 30%)**:黑鲸 V2 帧内角色占比 60%×56%,蓝鲸 89%×78%;素材规范化——按蓝鲸基准把黑鲸角色统一放大到 ~76% 高并居中,两皮肤显示大小一致;素材版本参数升至 `?v=3` 强制刷新。

## [0.4.17] - 2026-08-22


### 修复
- **V2 素材不生效(仍显示 V1 正面)**:宿主素材路由带 `immutable` 缓存,素材路径未变时浏览器沿用旧缓存;给全部素材 URL 加版本参数 `?v=2`,刷新后强制重新拉取——设置面板列表与界面浮层的小黑鲸均更新为 V2(偏 30°、去投影)形象。

## [0.4.16] - 2026-08-22


### 修复
- **自定义指令"被清空"、注入不生效的根因**:旧版(≤0.4.5)宠物配置存的是布尔 `enabled: true`,而 v0.4.6 起 schema 改为 `z.string()`;DSH 0.7.0 升级后 schemastery 校验变严格,`register(personal-center-pet)` 抛错导致**同一批注册的三个命名空间全部回滚**(自定义指令 / 价格 / 宠物都读不到,UI 显示空、系统提示词不注入)。
- **宠物 schema 兼容旧版布尔**:`enabled` 改为 `z.union([z.string(), z.boolean()])`,旧配置不再触发校验失败(显示层仍走 `normalizeEnabled` 归一化)。
- **注册隔离**:三个命名空间逐个注册并捕获失败——今后任一命名空间的脏值只跳过自己并打印警告,绝不再连累 `custom-instructions` 一起消失。

### 变更
- **小黑鲸素材升级 V2**:角色改为**正面偏 30°**(细节更丰富,与圆滚滚小蓝鲸同设计逻辑)、**去掉投影**;素材由 V2 帧重新合成(anim 313.6KB + idle 48.7KB,比 V1 更小);皮肤 id 不变(`black-whale`),配置无缝,客户端逻辑零改动。

## [0.4.15] - 2026-08-21


### 修复
- **宠物形象动画时的"边框"**:预览图去掉圆形裁切与背景——动画帧角色会位移/缩放(黑鲸动作帧缩小偏移、蓝鲸帧撑满宽度),圆形裁剪会产生明显的"框"感;改为方形透明显示,透明区与卡片底色融合,不再有边框感。

## [0.4.14] - 2026-08-21


### 修复
- **宠物形象动画时的灰边框**:预览图容器去掉浅灰背景(素材本身透明),动画动作幅度大时不再露出灰色圆形底边。

## [0.4.13] - 2026-08-21


### 变更
- **卡片形象动画改为交替播放**:黑鲸 / 小蓝鲸每次只有一张在动(约 2.2s),这张动完间隔后换另一张动(每 10s 轮换),不再一起动(更自然,不像设定好的)。

## [0.4.12] - 2026-08-21


### 变更
- **唤醒反馈移到左边大形象图**:宠物从「未启用 → 启用」的瞬间,左边大形象图播放一次随机动画(~2.2s)作为唤醒反馈;
- **删除右侧情绪气泡**(未启用时的「未启用」提示与启用后的表情图):开关本身已表达开/关,右侧气泡冗余;卡片结构 = 左边形象图 + 信息区 + 开关。

## [0.4.11] - 2026-08-21


### 新增
- **唤醒反馈**:宠物从「未启用 → 启用」的瞬间,右侧情绪气泡播放一次随机小动画(~2.2s),让用户明确感知"被唤醒了",随后回到当前情绪静态图。

## [0.4.10] - 2026-08-21


### 新增
- **宠物列表形象"活"起来**:面板打开期间,黑鲸/蓝鲸卡片左边的形象图**每 10s 随机播放一个小表情动画**(~2.2s,纯随机、与消耗数据无关);用户离开宠物面板即停止(定时器随组件卸载清理),省资源;启用/未启用卡都会动。

## [0.4.9] - 2026-08-21


### 变更
- **宠物卡片选中态不再加背景/蓝色边框**:启用状态完全由开关(on)表达,卡片本体无选中/hover 视觉(更贴近 DSH 原生交互)。

## [0.4.8] - 2026-08-21


### 变更
- **宠物卡片去掉 hover 背景/描边反馈**:交互只保留开关(点击/悬停卡片无任何视觉变化),符合 DSH 规范(原生设置无此 hover 交互);当前启用卡片保留品牌蓝边框 + 常驻浅背景作为状态标识。

## [0.4.7] - 2026-08-21


### 变更
- **未启用宠物卡片保持静态**:只有**已启用的宠物**走实时数据驱动(情绪/统计随 30s 轮询变化);未启用的卡片固定显示表情 + 「未启用」+ 占位统计,不再跟着数据一起动(面板更安静、省渲染)。

## [0.4.6] - 2026-08-21


### 修复
- **宠物开关互斥**:黑鲸 / 小蓝鲸各自独立开关,**只能启用一只**(配置 `enabled` 由 boolean 改为皮肤 id / 空字符串,`""` = 全部关闭);开启一只自动关闭另一只;旧配置(boolean)自动迁移。

### 变更
- 面板:点击宠物卡片 = 选中并启用该宠物(互斥);每张卡片的开关独立控制各自宠物(不再联动)。

## [0.4.5] - 2026-08-21


### 新增
- **第二皮肤「圆滚滚小蓝鲸」**(blue-whale,呼应 DeepSeek 蓝色品牌):设置 → 个人 → 宠物可切换 黑鲸 / 蓝鲸;素材按 `lib/pet-assets/<skin>/{animations,idle}/` 组织,宿主素材路由与配置 `skin` 字段支持双皮肤;即时切换,行为与黑鲸一致(5 情绪 / 常驻静止 / 情绪变化与逗弄动画)。

### 变更
- 素材目录重构:黑鲸素材迁移至 `lib/pet-assets/black-whale/`(移除旧的根目录 `animations/` `idle/` 结构)。

## [0.4.4] - 2026-08-20


### 变更
- 宠物名称统一为「**圆滚滚小黑鲸**」(原「浮游小黑鲸」);
- 行为补充:情绪变化时播放一次对应动画(~2.2s)后回待机静止(此前仅切换静态表情);行为描述统一为「常驻显示,平时静止不打扰,情绪变化时会播放对应动画,悬停/点击也会做小动作」;
- 面板 ⓘ 提示与文案同步更新;文档(README/DESKTOP-PET/DESIGN/DESIGN-SYSTEM/PROMOTION)同步。

## [0.4.3] - 2026-08-20


### 变更(开关样式 + 对抗式审查修复)
- **启用开关样式修正**:圆钮由深色文字色改为**白色 + 微投影**,轨道启用态为 DSH 品牌蓝、关闭态为浅灰(参考 DSH 交互色系),不再突兀;
- **右键「隐藏宠物」改为乐观应用**:先本地隐藏、再异步持久化,与面板开关行为一致(此前依赖保存成功,接口异常时隐藏无效);
- **宿主未重启提示**:宠物服务不可用时,面板显示「请重启应用」提示,不再"开了没反应";
- **旧版透明度值归一**:历史配置中的任意 opacity(如 50%)就近归一到档位(30%/60%/100%)并持久化,保证 UI 与宠物一致;
- **持久化串行队列**:修复多个配置写入并发时宿主"读-改-写"互相覆盖的缺陷(如面板归一 opacity 与拖拽 posOverride 同时发生)。

## [0.4.2] - 2026-08-20


### 变更(宠物面板极简,反馈收口)
- **移除位置四角选择与尺寸 S/M/L**:宠物固定 S 尺寸,位置完全由拖拽决定(拖到哪停哪、松手记忆),不再提供预设;
- **移除透明度滑块**:改为卡片下方「不透明度」三档按钮(30% / 60% / 100%),点击即应用(乐观),彻底避免"设不回来";
- **今日情绪改为卡片右侧小气泡**(当前情绪表情 + 名称,实时刷新),不再单独成块;
- **面板精简为单卡片**:预览 + 标题 + 今日统计 + 不透明度档位 + 右侧情绪气泡 + 启用开关;所有说明文字收入「ⓘ」悬停提示(DSH 主题化 popover),页面只剩一张卡片;
- **启用交互去重**:删除独立「启用宠物」复选框,只保留卡片右侧开关(点开使用、关闭即隐藏);
- 修复卡片内标题/描述对齐(标题上边距归零,统一垂直居中);
- 右键菜单仅保留「隐藏宠物」。

## [0.4.1] - 2026-08-20

### 变更(宠物行为重设计,参考 Codex 宠物设计规范)
- **默认静止不打扰**:去掉所有常驻循环动画(浮游/抖动/颤抖/呼吸),待机只显示**单帧静态表情**(新增 `pet-assets/idle/*.webp`,每张 ~14KB);
- **逗弄才动**:鼠标悬停或点击时,按当前情绪播放一次动作动画(~2 秒),播完回到静止;
- **自由拖拽**:拖到哪停哪,松手持久化;面板位置项增加「已拖拽到自定义位置」状态与「重置到右下角」按钮;
- **透明度修复**:滑块改为实时(onInput)+ 配置乐观应用(本地即时生效、异步持久化),可随意来回调节,不再出现"降下去升不回来";
- 宿主静态素材路由扩展为 `animations/` 与 `idle/` 两个白名单子目录。

## [0.4.0] - 2026-08-20

### 新增
- **桌面宠物「小黑鲸」**:界面右下角浮游角色,由真实用量数据驱动 5 种情绪(开心/忙碌/疲惫/钱包痛/打盹),30 秒轮询统计接口;点击吐随机数据气泡、可拖拽并记忆位置、右键快捷菜单(尺寸/隐藏);「宠物」tab 配置面板(启用/尺寸/位置/不透明度 + 今日情绪 + 阈值说明)。
- **动画素材**:5× 动画 WebP(真 alpha 透明,由 RGBA 帧合成,共 ~400KB),替代 GIF(二值透明白边),宿主经 `/personal-center/pet/assets/*` 提供,浏览器缓存。
- **今日成本桶**:统计接口新增 `cost.today`,驱动「钱包痛」情绪;未配置价格时为空对象,情绪自然降级。

### 变更
- 宠物配置走自有命名空间 `personal-center-pet`(环回路由 GET/POST `/personal-center/pet`),绕开设置白名单。
- 情绪判定顺序修正:打盹优先于开心(原组件在高缓存命中率下永远不会入睡)。

### 说明
- 情绪阈值已按本机真实数据校准(占位值低 100~5000 倍,会让鲸鱼永远「疲惫/忙碌」):开心=缓存≥70%、忙碌=今日工具调用≥400、疲惫=今日 Token≥2 亿、钱包痛=今日成本≥¥10、打盹=无活动≥10 分钟。
- 生效方式:**宿主端改动需重启应用**;客户端改动刷新页面即可。

## [0.3.0] - 2026-08-20

### 新增
- **成本估算**:按模型 × 每百万 token 单价估算成本,分币种(¥/$)分别合计;支持 DeepSeek 峰谷分时计价(北京 9:00-12:00、14:00-18:00 高峰);内置官方预设价(deepseek / kimi / gemini / gpt)+「提供方→模型」联想添加;展示本周/本月/累计。
- **缓存命中率**:整体命中率卡片 + 按模型命中率 + 会话回顾里的逐会话命中率。
- **会话回顾**:最近会话列表(标题/日期/时长/Token/缓存命中率),自动排除已归档会话。

### 变更
- 个人资料重构为子 tab(概览 / 回顾 / 模型成本),胶囊样式(选中淡圆角背景),页面不再过长。

### 修复
- 「应用官方预设」不再覆盖用户已填的 provider/key。
- 保存价格后「估算成本」卡立即刷新。
- Token 消耗统计计入 reasoning token,与成本口径一致。

## [0.2.0] - 2026-08-19

### 新增
- 「个人」分区:个人资料统计(真实数据,来自会话日志聚合)+ 个性化自定义指令。
- Token 活动热力图(每日/每周/累计)。

### 修复
- 个性化输入被清空(异步读取落地覆盖用户输入)。
- 保存失败根因:设置命名空间不在 Web 白名单,改用自有环回路由读写。

## [0.1.0] - 2026-08-19

### 新增
- 全局自定义指令(等价于 ChatGPT/Codex 的「个性化 → 自定义指令」),注入本机所有聊天。
