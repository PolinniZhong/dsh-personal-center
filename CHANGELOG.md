# 变更日志(Changelog)

本项目的版本历史。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/),版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.4.1] - 2026-08-20

### 变更(宠物行为重设计,参考 Codex 宠物设计规范)
- **默认静止不打扰**:去掉所有常驻循环动画(浮游/抖动/颤抖/呼吸),待机只显示**单帧静态表情**(新增 `pet-assets/idle/*.webp`,每张 ~14KB);
- **逗弄才动**:鼠标悬停或点击时,按当前情绪播放一次动作动画(~2 秒),播完回到静止;
- **自由拖拽**:拖到哪停哪,松手持久化;面板位置项增加「已拖拽到自定义位置」状态与「重置到右下角」按钮;
- **透明度修复**:滑块改为实时(onInput)+ 配置乐观应用(本地即时生效、异步持久化),可随意来回调节,不再出现"降下去升不回来";
- 宿主静态素材路由扩展为 `animations/` 与 `idle/` 两个白名单子目录。

## [0.4.0] - 2026-08-20

### 新增
- **桌面宠物「小黑鲸」**:界面右下角浮游角色,由真实用量数据驱动 5 种情绪(开心/忙碌/疲惫/钱包痛/打盹),30 秒轮询统计接口;点击吐随机数据气泡、可拖拽并记忆位置、右键快捷菜单(尺寸/隐藏);「宠物」tab 配置面板(启用/尺寸/位置/不透明度 + 今日情绪 + 阈值说明)。
- **动画素材**:5× 动画 WebP(真 alpha 透明,由 RGBA 帧合成,共 ~400KB),替代 GIF(二值透明白边),宿主经 `/personal-center/pet/assets/*` 提供,浏览器缓存。
- **今日成本桶**:统计接口新增 `cost.today`,驱动「钱包痛」情绪;未配置价格时为空对象,情绪自然降级。

### 变更
- 宠物配置走自有命名空间 `personal-center-pet`(环回路由 GET/POST `/personal-center/pet`),绕开设置白名单。
- 情绪判定顺序修正:打盹优先于开心(原组件在高缓存命中率下永远不会入睡)。

### 说明
- 情绪阈值已按本机真实数据校准(占位值低 100~5000 倍,会让鲸鱼永远「疲惫/忙碌」):开心=缓存≥70%、忙碌=今日工具调用≥400、疲惫=今日 Token≥2 亿、钱包痛=今日成本≥¥10、打盹=无活动≥10 分钟。
- 生效方式:**宿主端改动需重启应用**;客户端改动刷新页面即可。

## [0.3.0] - 2026-08-20

### 新增
- **成本估算**:按模型 × 每百万 token 单价估算成本,分币种(¥/$)分别合计;支持 DeepSeek 峰谷分时计价(北京 9:00-12:00、14:00-18:00 高峰);内置官方预设价(deepseek / kimi / gemini / gpt)+「提供方→模型」联想添加;展示本周/本月/累计。
- **缓存命中率**:整体命中率卡片 + 按模型命中率 + 会话回顾里的逐会话命中率。
- **会话回顾**:最近会话列表(标题/日期/时长/Token/缓存命中率),自动排除已归档会话。

### 变更
- 个人资料重构为子 tab(概览 / 回顾 / 模型成本),胶囊样式(选中淡圆角背景),页面不再过长。

### 修复
- 「应用官方预设」不再覆盖用户已填的 provider/key。
- 保存价格后「估算成本」卡立即刷新。
- Token 消耗统计计入 reasoning token,与成本口径一致。

## [0.2.0] - 2026-08-19

### 新增
- 「个人」分区:个人资料统计(真实数据,来自会话日志聚合)+ 个性化自定义指令。
- Token 活动热力图(每日/每周/累计)。

### 修复
- 个性化输入被清空(异步读取落地覆盖用户输入)。
- 保存失败根因:设置命名空间不在 Web 白名单,改用自有环回路由读写。

## [0.1.0] - 2026-08-19

### 新增
- 全局自定义指令(等价于 ChatGPT/Codex 的「个性化 → 自定义指令」),注入本机所有聊天。
