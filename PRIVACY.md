# 隐私说明(Privacy)

`dsh-personal-center` 是一个**本地运行**的 DeepSeek Harness 插件,所有数据都在你自己的设备上处理,不向任何第三方服务器上传。

## 数据读取与存储

### 自定义指令(个性化)

- 你在「个性化」里输入的文本,保存在**本机**的 `settings.yaml`(`<DSH_HOME>/settings.yaml`);
- 它会随每次请求作为系统提示词的一部分发给**你已配置的模型服务商**(与 DeepSeek Harness 自身发送请求的方式一致,不增加新的传输路径)。

### 个人资料统计

- 统计只读取本机会话日志(`<DSH_HOME>/data/dsh/sessions/**/session.jsonl.zstd`);
- **只聚合数字**,例如:Token 数量、会话数、工具调用次数、模型名、时间戳;
- **不读取、不存储、不展示任何聊天正文内容**(你的消息、AI 回复、文件内容都不会被统计模块读取)。

### 外观(全局字号)

- 「外观」tab 的全局字号只影响界面文字大小,数值保存在**本机**浏览器 `localStorage`(`dsh-personal-center.uiFont`);
- 字号改动只在本机页面即时生效,不读取、不存储、不上传任何数据。

### 桌面宠物

- 宠物情绪只使用**聚合数字**(今日 Token / 工具调用 / 成本、缓存命中率、最近活动时间),不读取聊天正文;
- 宠物配置(`enabled/size/position/拖拽位置/不透明度`)与动画素材全部保存在**本机**(`settings.yaml` 命名空间 `personal-center-pet` + 插件包内素材),由宿主环回路由提供,不上传任何数据。

## 不联网 / 不收集

- 插件的 HTTP 接口(`/personal-center/*`)仅监听 **127.0.0.1 回环地址**,只允许本机访问;
- **无遥测、无分析、无崩溃上报、无第三方数据收集**;
- 不会把任何统计结果或你的指令发送给插件作者或其他服务。

## 数据归属与删除

- 所有数据留在本机,归你所有;
- 停用 / 卸载本插件不会删除你的会话日志或 `settings.yaml` 中的指令文本;如需彻底删除,请手动清理相应文件;
- 统计结果每次打开时现场计算(带 60 秒内存缓存),不落盘生成新的统计文件。

## 权限边界

- 本插件不申请、也不需要任何网络访问权限(除 DSH 自身发往模型服务商的请求);
- 不读取环境变量中的凭据,不读取其他应用的数据。

如有隐私方面的疑问,欢迎在仓库提 issue。
